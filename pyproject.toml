[project]
name = "mcp-secure-agents"
version = "0.2.0"
description = "Secure MCP agent demo with policy enforcement and GigaChat tool calling"
requires-python = ">=3.10"

dependencies = [
    "mcp[cli]>=1.24,<2",
    "pydantic>=2.0",
    "PyYAML>=6.0",
    "langchain>=1.0,<2",
    "langchain-gigachat",
    "langchain-mcp-adapters",
    "langgraph",
]

[tool.uv]
dev-dependencies = []
