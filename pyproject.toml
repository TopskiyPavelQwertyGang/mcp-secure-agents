[project]
name = "mcp-secure-agents"
version = "0.1.0"
description = "Educational secure MCP agent examples"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "pydantic>=2.0",
]

[tool.uv]
dev-dependencies = []
